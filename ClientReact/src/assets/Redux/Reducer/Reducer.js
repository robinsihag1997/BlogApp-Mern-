import { intialStates } from "./InitalState";

function mainReducer(state = intialStates, action) {
  switch (action.type) {
    case "add":
      break;

    default:
      break;
  }
}

export default mainReducer;
